apiVersion: v1
kind: Service
metadata:
  name: {{ name }}
  namespace: {{ namespace }}
  labels:
    app: {{ name }}
spec:
  selector:
    app: {{ name }}
  ports:
    - port: 80
      targetPort: 8080
      name: http
  type: ClusterIP
